{% extends 'layout.html.twig' %}

{% block content %}
    <h1>{{ team.name }}</h1>
    <ul>
        {% for player in players %}
            <li>{{ player.firstName }}</li>
        {% endfor %}
    </ul>
        
{% endblock %}
